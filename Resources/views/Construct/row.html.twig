{% import "BluemesaSiteTemplateBundle:Macros:list.html.twig" as l %}

<td>{{ l.link(entity, 'bluemesa_construct_show', false, entity.id) }}</td>
<td>{{ entity.name }}</td>
<td>{{ entity.type }}</td>
<td>{{ entity.size }}kb</td>
<td>{{ entity.resistances | join(', ') }}</td>
